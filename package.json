{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "description": "Sample package for the release workflow demo",
  "scripts": {
    "release": "echo 'release via GitHub Actions'"
  }
}
